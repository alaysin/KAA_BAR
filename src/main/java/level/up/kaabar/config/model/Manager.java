package level.up.kaabar.config.model;

public class Manager extends User {
    public Manager(String login, String password, boolean isAdmin, String name, String surname) {
        super(login, password, isAdmin, name, surname);
    }
}
